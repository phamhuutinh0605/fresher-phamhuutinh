import React from "react";

const CancelButton = () => {
  return (
    <button
      type="button"
      className="btn btn-default btn__cancel"
      style={{ marginLeft: 10 }}
    >
      Cancel
    </button>
  );
};

export default CancelButton;
